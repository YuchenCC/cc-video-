# Scene 002 — 接入篇

- 时长：37.5 秒。
- 章节 Beat：0–2.5 秒使用 `TitleTransition`，文本为“接入篇”。
- 核心概念：依赖与配置完成低侵入接入，所有入口复用同一套规则。
- 构图：左侧 Spring Boot 项目，右侧依赖、配置、三入口和规则引擎。
- Beat：章节标题 → 引入依赖 → 少量配置 → 自动生效 → 日志与特殊入口 → 统一规则。
- 主动画：标题滑入、接入步骤级联、流程线绘制、三入口依次聚合。
- 转场：由根构图在下一场景边界执行 `wipeSoft`。
- 验收：明确表达不逐个修改 Controller、不改变返回对象。
