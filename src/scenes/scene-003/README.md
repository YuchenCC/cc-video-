# Scene 003 — 配置篇

- 时长：34.5 秒。
- 章节 Beat：0–2.5 秒使用 `TitleTransition`，文本为“配置篇”。
- 核心概念：基础、增强、例外和自定义四层配置覆盖不同需求。
- 构图：左侧四层 `LayerStack`，右侧字段、复杂规则、例外和插件统计。
- Beat：章节标题 → 四层展开 → 常见字段 → 增强规则 → 例外记录 → 自定义策略。
- 主动画：层级堆叠、字段标签级联、三类扩展依次入场、统计卡聚焦。
- 验收：四层能力边界清楚，自定义能力仍连接统一统计。
