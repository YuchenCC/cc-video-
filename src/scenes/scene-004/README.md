# Scene 004 — 日志篇

- 时长：53.5 秒。
- 章节 Beat：0–2.5 秒使用 `TitleTransition`，文本为“日志篇”。
- 核心概念：日志脱敏要按日志形态选择处理路径；有字段名复用接口规则，无字段名启用有限兜底，并控制性能边界。
- 字幕：保留底部字幕与 `scripts/subtitles.md` 中的 scene-004 时间。
- 构图：浅灰白板 + concept 三列区域；Terminal 作为连续主物件，左侧承载来源/边界卡片，右侧承载屏幕文字和人物反馈。
- 正文 Beat：2.5 秒后开始，按用户分镜顺序轻微压缩进剩余时长。
  - 大 Terminal 滚动到漏脱敏手机号，`keyboard.png` 在下方，右侧 `8.png` 表示发现异常。
  - 上游、当前、下游三类来源依次进入统一日志窗口，右侧显示“日志来源 ≠ 只有本系统”。
  - 6 张字段卡翻页，第二页和第五页以红色虚线表示人工漏掉字段。
  - 中间脱敏规则卡承接接口输出和日志输出，说明有字段名时复用接口规则。
  - 接口与日志 Terminal 穿过规则卡后停到右侧，同字段同步变成脱敏结果。
  - 无字段文本 Terminal 接入“兜底识别器引擎”，手机号、邮箱、证件号被框选。
  - 手机号、证件号、邮箱三个标志依次点亮并汇入 Terminal，对应内容进入脱敏状态。
  - 非全量扫描、只处理明确类型、考虑性能影响三张卡片指向安全日志 Terminal。
  - 约 51s 保持画面状态，右侧出现 `assets/characters/39.png` 收束讲解。
- 主动画：日志滚动、风险圈选、来源汇入、字段卡翻页、规则复用连线、Terminal 平移穿过规则卡、兜底引擎植入、类型标志汇入、边界卡片指向 Terminal。
- 验收：字幕不被遮挡；主要内容不进入底部安全区；明确表达“兜底不是全量扫描，只处理边界明确的数据类型”。
