# Scene 005 — 治理篇

- 时长：76.5 秒。
- 章节 Beat：0–2.5 秒使用 `TitleTransition`，文本为“治理篇”。
- 核心概念：运行时线索和人工确认构成持续治理闭环。
- 录屏 Beat：2.5–66.7931 秒使用 `concept-expansion` 三分区白板构图。中左大方框连续播放 `assets/video/luping1.mp4` 与 `assets/video/luping2.mp4`，右区持续展示 `assets/characters/18.png` 承接“为什么、如何判断”的问题语义。
- 录屏：`luping1` 播放 27.5 秒，随后无缝切换到时长 36.7931 秒的 `luping2`；两段均静音并使用 `object-fit: contain` 保留完整界面，现有旁白和字幕时间不变。
- 衔接：录屏层覆盖至 66.7931 秒，旧动画仍按原时间轴在下层运行；第二段录屏结束后直接露出第五幕剩余约 9.7 秒的原画面，等待下一轮重制。
- 后续构图：人工清单与运行时线索对比，中部五步循环，底部报告和隐私说明。
- 后续人物：`assets/characters/9.png` 保留原时间轴，在下层短暂表达人工排查疲惫。
- Beat：章节标题 → 录屏演示与思考人物 → 运行时线索 → 报告建议 → 确认配置 → 持续观察 → 隐私边界。
- 主动画：录屏框与人物轻柔入场；后续清单强调、线索卡入场、循环步骤依次高亮、隐私卡锁定保持原时间点。
- 验收：治理闭环和“不保存敏感原文”必须同时清晰。
