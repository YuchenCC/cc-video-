# Scene 005 — 治理篇

- 时长：76.5 秒。
- 章节 Beat：0–2.5 秒使用 `TitleTransition`，文本为“治理篇”。
- 核心概念：运行时线索和人工确认构成持续治理闭环。
- 录屏 Beat：2.5–59.8015 秒使用 `concept-expansion` 三分区白板构图。中左大方框连续播放 `assets/video/luping3.mp4` 与 `assets/video/luping2.mp4`，右区下方持续展示 `assets/characters/18.png` 承接“为什么、如何判断”的问题语义，右区上方展示轻量白板提示卡。
- 录屏：`luping3` 按视频流时长播放 20.5084 秒，随后无缝切换到时长 36.7931 秒的 `luping2`；两段均静音并使用 `object-fit: cover` 填满当前 video shell，现有旁白和字幕时间不变。
- 白板提示：参考白底、浅蓝边框、圆角卡片样式，重构为上下两个独立卡片：上方“组件线上演示”标题栏固定不变，下方说明区按时间点翻页展示。每条说明结束前向左平移并淡出隐藏，再切入下一条。第一段录屏按视频内 0/4/10/15 秒切换为“演示脱敏实验室用于汇总当前配置规则和测试”“演示单字段规则下脱敏能力”“演示 Java 对象在规则下脱敏能力”“演示无规则文本兜底脱敏能力”；第二段录屏按视频内 0/6/18/28 秒切换为“可生成脱敏报告，包含：脱敏次数、覆盖接口、平均耗时、失败次数等”“以 API 统计各维度的脱敏类型、脱敏次数，提供风险建议”“脱敏规则建议会抓取兜底脱敏的信息，提供脱敏配置补充建议”“额外收集性能数据，观测脱敏实际工作情况”。
- 衔接：`luping2` 提前至第五幕 23.0084 秒开始，录屏层覆盖至 59.8015 秒；第二段录屏结束后露出参考图式白色大屏治理闭环。
- 后续构图：纯白 concept 模板背景上的白色大屏，框内直接使用 `assets/images/summary.png` 展示完整“持续治理闭环”总结图。
- 后续人物：`assets/characters/17.png` 位于右侧，承接治理闭环形成后的完成和振奋语义。
- Beat：章节标题 → 录屏演示与思考人物 → 运行时线索 → 报告建议 → 确认配置 → 持续观察 → 隐私边界。
- 主动画：录屏框、白板与人物轻柔入场；白板标题栏固定，下方说明页按录屏时间翻入，并在结束前向左平移到中心动线后隐藏；录屏结束后大屏与总结图整体入场。
- 验收：治理闭环和“不保存敏感原文”必须同时清晰。
