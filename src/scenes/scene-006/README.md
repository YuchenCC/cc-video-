# Scene 006 — AI 工程化

- 时长：69.5 秒。
- 章节 Beat：0–2.5 秒使用 `TitleTransition`，文本为“AI 工程化”。
- 正文时间基准：正文音频从场景内部 2.5 秒开始，对应 2.5–69.115 秒；末尾项目总结截图 Beat 承接口播收束与致谢。
- 核心概念：AI 生成内容必须经过需求拆解、分层验证和长期约束，才能形成可追踪、可运行、可验证的交付。
- 构图：统一使用 `concept-expansion` 浅色白板语言，从版本路线图逐步演化为任务流程、质量金字塔和交付闭环。

## Beat

1. **2.5–13.5 秒｜交付版本路线图**
   - 卡片内容与路线图参考逐项对应：MVP「接入可行性」、R2「扩展与治理能力」、R2.5「模拟系统接入组件」、R2.6「Dashboard 工作台」、R3「未来版本 / 规划方向」。
   - MVP 先在中轴出现；R2 从 MVP 右侧衍生并停稳，二者一起左移让 R2 接管中轴。
   - R2 再衍生 R2.5，三者一起左移让 R2.5 接管中轴；随后依次从右侧生成 R2.6 和 R3。
   - 底部迭代链使用“反馈 / 问题 → PRD → Issue → 实现 → 测试 → 文档记忆 → 下一轮”。
   - 结尾重新聚焦 MVP 节点，连续交接到需求拆解 Beat。
2. **13.5–26 秒｜需求拆解**
   - MVP 节点本体直接移动到说明书落点，落位后原位形变为“需求说明书”，副标题为“MVP版本”。
   - 说明书以树状关系横向拆出三个 Issue：接入能力、治理规则、结果展示。
   - 每个 Issue 再拆出两个 Task；右侧出现“交付验收”组件，六个 Task 依次飞入并点亮验收进度。
   - 所有拆解结构清空后，“可交付验收”组件移动到模块级测试落点，作为质量验收 Beat 的形变起点。
3. **26.2–39 秒｜质量验收结构**
   - “可交付验收”先移动，整片 4:15 到达模块级测试位置后再开始形变并替换文字为“模块级测试”。
   - 三层结构依次出现：模块级测试、集成级测试、黑盒验收。
   - 左侧三层文字照搬参考图：模块级测试覆盖 `core、log4j2、report`，集成级测试覆盖 `starter、dashboard starter、demo`，黑盒验收从浏览器和接口视角验证 Demo 工作台、Dashboard、风险画像、日志建议、报告中心。
   - 右侧“关键结果”面板按 `mvn test`、`demo / starter / dashboard`、`P0 22/22`、`P1 20/20`、`P2 10/16` 逐项出现；每一项在 `CheckBadge` 打勾的同时展示右侧文字。
4. **39–55 秒｜约束驱动的交付闭环**
   - 画面改为三节点闭环：持续记忆 → 验收闭环 → 可验证交付 → 持续记忆。
   - 对应整片 4:27，先在屏幕中央生成“持续记忆”卡片；卡片内三条文字陆续出现，到整片 4:32 完成。
   - 持续记忆卡片随后整体向上移动到原闭环顶部位置，闭环底轨出现。
   - 验收闭环承接每项能力的运行结果；可验证交付收束为可追踪、可运行、可验证。
   - 验收闭环卡片出现后绘制第一段指向箭头；可验证交付卡片出现后继续绘制连接箭头，并补上回到持续记忆的闭环箭头。
5. **55–69.5 秒｜项目总结截图**
   - 参考整片 3:33 的 scene-005 大屏布局，在白色大屏框中嵌入 `assets/images/project-summary.png`。
   - 右侧使用 `assets/characters/39.png` 作为举手讲解者，指向总结页重点。
   - 对应整片 4:43，使用 `wipeSoft` 从闭环 Beat 切入；大屏和截图轻微淡入放大后稳定展示至片尾。
   - 截图完整展示，不裁切，不额外叠加说明文字。

## 动画与转场

- MVP 节点是路线图到需求说明书的连续迁移锚点。
- 路线与树状关系使用 `arrowDraw`；Issue / Task 使用错峰衍生；质量分层从模块级测试开始依次展开。
- 前三个 Beat 使用对象聚焦和 `localReplace` 连续推进；质量验收到交付闭环在整片 4:27 使用一次 `wipeSoft`。
- 最终三节点保持完整对比度，闭环箭头轻量加粗提示关系已收束。
- 项目总结截图 Beat 复用 scene-005 的大屏截图入场节奏，只做轻量 `fadeIn + scale`。

## 验收

- 不听口播也能理解“计划 → 拆解 → 验证 → 交付”的关系。
- MVP 到需求说明书的移动连续，Task 汇入交付验收后画面收束明确。
- 金字塔层级、结果面板和最终三项交付状态均在字幕安全区之上。
- 项目总结截图完整可读，并保持在大屏框内。
- 参考图只提供信息结构，最终视觉必须保持项目的白板式蓝绿技术图解语言。
